#include <bits/stdc++.h>
using namespace std;
int main()
{
    int n;
    cin >> n;
    vector<int> v(n);
    for (int i = 0; i < n; i++)
    {
        cin >> v[i];
    }

    reverse(v.begin(), v.end());
    vector<int> u;

    for (int i = 0; i < n; i++)
    {
        if (find(u.begin(), u.end(), v[i]) == u.end())
        {
            u.push_back(v[i]);
        }
    }

    cout << u.size() << endl;
    reverse(u.begin(), u.end());
    for (int val : u)
    {
        cout << val << " ";
    }
    cout << endl;
    return 0;
}

/*
learning :
    -> ekti vector e multiple duplicate thakle sheser ta thik rekhe prothom
        duplicate remove kore fele diye tarpor unique vector ta print korbo

ধরা যাক ইনপুট:
v = [1, 5, 5, 1, 6, 1]

আমরা চাই:
➡ প্রতিটি উপাদানের *শুধু শেষের occurrence রাখবো*
➡ আগের ডুপ্লিকেটগুলো ✗ দিয়ে মুছে ফেলবো
➡ যেগুলো রাখা হবে সেগুলো √ দিয়ে দেখাবো

Step by Step বিশ্লেষণ:

Index: 0 → Value = 1
    🔍 v[0] = 1 → পরে আরও আছে → ✗  (ডিলিট)

Index: 1 → Value = 5
    🔍 v[1] = 5 → পরে আরও আছে → ✗  (ডিলিট)

Index: 2 → Value = 5
    🔍 v[2] = 5 → পরে আর নেই → √  (push_back)

Index: 3 → Value = 1
    🔍 v[3] = 1 → পরে আরও আছে → ✗  (ডিলিট)

Index: 4 → Value = 6
    🔍 v[4] = 6 → পরে আর নেই → √  (push_back)

Index: 5 → Value = 1
    🔍 v[5] = 1 → শেষেরটি → √  (push_back)

🔚 Final Result Vector: [5, 6, 1]

✔ এখানে √ দিয়ে বোঝানো হয়েছে যেগুলো push_back() হয়েছে রেজাল্টে
❌ আর ✗ দিয়ে বোঝানো হয়েছে যেগুলো মুছে (erase) দেওয়া হয়েছে

এইভাবেই আমরা শুধুমাত্র *rightmost unique elements* রেখে final result তৈরি করি।

*/